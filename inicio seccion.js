function login (){
    var  user, pass;
  
    user = document.getElementById("Usuario").value;
    user = document.getElementById("Contraseña").value;

    if( user === "mishelle" && pass === "1234") 
    if( user === "Anto" && pass === "1234"){

        window.location = "Rutas.html";
    }
}